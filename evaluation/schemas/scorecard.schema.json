{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Scorecard",
  "type": "object",
  "required": ["judgeId", "date", "results"],
  "properties": {
    "judgeId": { "type": "string", "enum": ["A", "B", "C"] },
    "date": { "type": "string", "format": "date" },
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["taskId", "outputA", "outputB", "preference"],
        "properties": {
          "taskId": { "type": "string" },
          "outputA": { "$ref": "#/$defs/rating" },
          "outputB": { "$ref": "#/$defs/rating" },
          "preference": { "type": "string", "enum": ["A", "B", "TIE"] }
        }
      }
    }
  },
  "$defs": {
    "rating": {
      "type": "object",
      "required": ["scores"],
      "properties": {
        "scores": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 1, "maximum": 5 }
        },
        "notes": { "type": "string" }
      }
    }
  }
}
